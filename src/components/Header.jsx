// Using 'dsznajder.es7-react-js-snippets' extension for snippets
// Avoiding import React in every created component, just use undercoscore before the snippet shortcut
// like instec 'rfce' use '_rfce'

function Header({ text }) {
  return (
    <header>
      <div className="className">
        <h2>{text}</h2>
      </div>
    </header>
  )
}

export default Header